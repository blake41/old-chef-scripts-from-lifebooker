check process memcached
  with pidfile /var/run/memcached.pid
  start program = "/bin/su -c 'service memcached start'"
  stop program = "/bin/su -c 'service memcached stop'"
  if 5 restarts within 5 cycles then timeout
