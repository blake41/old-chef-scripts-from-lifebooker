check process apache
  with pidfile /var/run/apache2.pid
  start program = "/bin/su -c 'service apache2 start'"
  stop program = "/bin/su -c 'service apache2 stop'"
  if 5 restarts within 5 cycles then timeout
